# 開発フロー（詳細）

**中国語会話学習アプリ（ブラウザ版）**
エージェント駆動開発（Codex / Claude Code）による、タスク単位の開発・レビュー・デプロイの手順を詳細化する。

| 項目 | 内容 |
|------|------|
| 文書バージョン | v1.0 |
| 対象企画書 | 企画書.md v2.0 |
| 対象仕様書 | 詳細仕様書.md |
| 作成日 | 2026-09-06 |
| 開発ツール | VS Code + Codex / Claude Code |
| デプロイ先 | Cloudflare（Workers + Pages） |

---

## 1. 開発の基本原則

1. **仕様駆動**: 詳細仕様書に定義された機能を、1機能ずつ実装する。
2. **小さなタスク単位**: 1タスク＝1機能・1修正。大きな変更は分割する。
3. **自動レビュー必須**: コードは必ず `git diff` レビューを通してからコミットする。
4. **規約の一元管理**: プロジェクト規約は `AGENTS.md`（Codex 用）と `CLAUDE.md`（Claude Code 用）に集約。
5. **セキュリティ最優先**: API キーは Workers Secrets のみ。リポジトリ・ブラウザに保存しない。
6. **デプロイは `wrangler`**: ビルド確認 → テスト → デプロイの順。

---

## 2. プロジェクト初期セットアップ

### 2.1 リポジトリ構成
```
chinese-learning-app/
├─ 企画書.md          # 企画（v2.0）
├─ 詳細仕様書.md      # 詳細設計
├─ 用語定義書.md      # 用語集
├─ 開発フロー.md      # 本ファイル
├─ AGENTS.md          # Codex 用規約
├─ CLAUDE.md          # Claude Code 用規約
├─ web/               # フロント（React + Vite + TS）
├─ worker/            # Cloudflare Workers（Hono）
│  └─ src/
│     ├─ index.ts     # エントリ
│     ├─ routes/      # /api/chat, /transcribe, /speak, /config
│     └─ lib/         # LLM, STT, TTS, config
└─ package.json       # ワークスペース管理
```

### 2.2 初期化コマンド
```bash
# ワークスペース
npm init -y
# フロント（Vite + React + TS + Tailwind）
npm create vite@latest web -- --template react-ts
cd web && npm install tailwindcss @tailwindcss/vite && cd ..
# Worker（Hono）
npm create cloudflare@latest worker -- --framework=honc
# PWA プラグイン
cd web && npm install -D vite-plugin-pwa && cd ..
```

### 2.3 規約ファイル（AGENTS.md / CLAUDE.md）の作成
初期セットアップ時に、以下の内容を必ず記載する。

```markdown
# プロジェクト規約
## 技術
- フロント: React + Vite + TypeScript（strict）
- バックエンド: Cloudflare Workers + Hono
- UI: Tailwind CSS
- 型付けを厳守。any を避ける。

## 命名
- 用語定義書の英語表記（Friend, hskLevel, Voice 等）を使う。

## コマンド
- ビルド: npm run build（web / worker）
- デプロイ: wrangler deploy
- テスト: npm test

## 規約
- 1タスク=1コミット。
- コミット前に git diff レビュー。
- API キーは Secrets。コミット禁止。
```

---

## 3. タスク管理ルール

### 3.1 タスクの粒度
| 例 | 粒度判定 |
|----|----------|
| /api/chat を実装する | ○ 1タスク |
| 会話画面の UI と API と設定画面を全部作る | × 分割する |
| 音声認識のエラー時フォールバックを追加 | ○ 1タスク |
| プロバイダ設定 + 音声カスタマイズ + 履歴を同時に | × 分割する |

### 3.2 タスク番号
- タスクは `T-<番号>` で管理（例: `T-01`, `T-02`）。
- チケット（GitHub Issues 等）を立てる場合は本文冒頭に `T-XX` を付ける。

### 3.3 タスクの定義書式
```
T-XX: <機能名>
目的: <なぜ>
仕様: 詳細仕様書 §<節>
受け入れ条件:
  - [ ] <条件1>
  - [ ] <条件2>
```

---

## 4. エージェント駆動開発の標準ループ

各タスクを以下のサイクルで進める。

```
① タスク定義 → ② エージェント委譲 → ③ 自動レビュー → ④ 修正 → ⑤ コミット
                                        ↑__________③' 不合格時__________|
```

### 4.1 ① タスク定義
- 詳細仕様書の該当節を読み、受け入れ条件を明確にする。
- エージェントに渡すプロンプトを書く（4.2 参照）。

### 4.2 ② エージェント委譲
#### Codex（推奨: 対話型・高速）
```bash
codex exec 'T-01: 詳細仕様書.md の 5.1 に従い POST /api/chat を worker/src に実装。
  - Hono でルーティング、LLM プロキシ、構造化JSON検証。
  - AGENTS.md の規約に従う。テスト追加。' \
  --sandbox workspace-write
```
- 必ず `--sandbox workspace-write` でワークスペース内のみ変更。
- 長いタスクはバックグラウンド実行（`background=true, pty=true`）で監視。

#### Claude Code（推奨: 深い推論・複雑な設計）
```bash
claude -p 'T-01: ...' \
  --allowedTools 'Read,Edit,Write,Bash' \
  --max-turns 15
```
- 複雑な設計・複数ファイルをまたぐ変更は Claude Code を推奨。
- `--allowedTools` で権限を絞る。

### 4.3 ③ 自動レビュー（必須）
コミット前に必ずレビューを通す。

#### レビューの実行
```bash
# Codex でレビュー
codex review --base main

# Claude Code に diff を投げる
git diff | claude -p 'この変更をレビュー。バグ・セキュリティ・仕様との不整合を指摘。' --max-turns 1

# Hermes 経由（本環境）
# レビュー結果を確認し、問題があれば修正
```

#### レビュー観点
- 詳細仕様書の要件を満たすか
- セキュリティ（キー漏洩・入力検証）問題がないか
- 型安全（strict）を守るか
- 用語定義書の命名を使うか
- テストが通るか

### 4.4 ④ 修正
- レビューで指摘された事項をエージェントに修正させる。
- 合格するまで ③→④ を繰り返す。

### 4.5 ⑤ コミット
```bash
git add .
git commit -m "T-01: POST /api/chat を実装"
```
- 1タスク＝1コミット。メッセージは先頭に `T-XX:` を付ける。
- コミット後にテスト実行を確認。

---

## 5. ブランチ・バージョン管理

### 5.1 ブランチ戦略
| ブランチ | 用途 |
|----------|------|
| `main` | 本番安定版（常にデプロイ可能） |
| `feature/T-XX` | 機能開発（タスク単位） |
| `fix/T-XX` | バグ修正 |

### 5.2 フロー
1. `main` から `feature/T-XX` を作成
2. 標準ループ（§4）で開発
3. レビュー合格後 `main` にマージ
4. 本番デプロイ

### 5.3 コミットメッセージ規約
| 形式 | 用途 |
|------|------|
| `T-01: ...` | 新機能 |
| `fix: ...` | バグ修正 |
| `docs: ...` | ドキュメント |
| `refactor: ...` | リファクタリング |
| `chore: ...` | その他 |

---

## 6. テスト方針

### 6.1 テストの種類
| 種類 | 対象 | 実行タイミング |
|------|------|----------------|
| 単体テスト | LLM 応答スキーマ、音声マッピング | タスク完了時 |
| 統合テスト | /api/chat, /api/speak, /api/transcribe | 各フェーズ完了時 |
| E2E | 会話フロー（テキスト→音声） | リリース前 |
| 実機確認 | iOS Safari / Android Chrome の音声API | リリース前 |

### 6.2 実行コマンド
```bash
# Worker テスト
cd worker && npm test
# フロントのビルドチェック
cd web && npm run build
```

### 6.3 テスト記述の規約
- テストは詳細仕様書のスキーマを直接参照して書く（仕様とテストの乖離を防ぐ）。
- LLM の応答はモック化し、ネットワークに依存しない。

---

## 7. デプロイフロー

### 7.1 デプロイ手順
```bash
# 1. フロントのビルド確認
cd web && npm run build

# 2. Worker のテスト
cd worker && npm test

# 3. 本番デプロイ
cd worker && wrangler deploy

# 4. フロントの配信（Pages / Workers Assets）
wrangler pages deploy web/dist
```

### 7.2 シークレット管理（重要）
```bash
# API キーを Workers Secrets に登録（リポジトリに入れない）
wrangler secret put OPENAI_API_KEY
wrangler secret put ELEVENLABS_API_KEY
```
- キーはすべて `wrangler secret put` で登録する。
- `.env`・リポジトリへのコミットは厳禁。

### 7.3 デプロイ前チェックリスト
- [ ] `npm run build` が通る
- [ ] `npm test` が通る
- [ ] シークレットが登録済み
- [ ] 用語定義書の命名を守っている
- [ ] API キーがコード・履歴に含まれない

---

## 8. フェーズ別タスク一覧（M1〜M4）

### M1: 会話（テキスト）＋ HSK 制御
| タスク | 内容 | 受け入れ条件 |
|--------|------|--------------|
| T-01 | POST /api/chat を実装（LLM プロキシ・構造化JSON） | バイリンガル応答+添削+語彙を返す |
| T-02 | HSK 級別制御のプロンプト設計 | HSK{級}の語彙・文法で返答 |
| T-03 | 会話画面 UI（履歴・表示・HSKバッジ） | メッセージ表示、ストリーミング |
| T-04 | オンボーディング（趣味・HSK級・ペルソナ） | 初期設定で会話を開始できる |
| T-05 | 友達（ペルソナ）の作成・一覧 | 趣味別ペルソナを作成・選択できる |
| T-06 | Cloudflare デプロイ | URL で動作確認 |

### M2: 音声（TTS/STT）＋ 音声カスタマイズ
| タスク | 内容 | 受け入れ条件 |
|--------|------|--------------|
| T-07 | 中国語 TTS（Web Speech API） | 返答を中国語で再生 |
| T-08 | 音声カスタマイズ UI（質・性別） | 質・性別を変更できる |
| T-09 | POST /api/speak（音声属性マッピング） | 属性に応じた音声を返す |
| T-10 | 中国語 STT（Web Speech API） | 発話を中国語テキスト化 |
| T-11 | POST /api/transcribe | 音声→テキストを返す |

### M3: BYO-AI 設定
| タスク | 内容 | 受け入れ条件 |
|--------|------|--------------|
| T-12 | GET/PUT /api/config | 設定の取得・保存 |
| T-13 | 設定画面（言語別STT/TTS・LLM） | 言語ごとにプロバイダ設定可 |
| T-14 | シークレット登録フロー | キーがブラウザに残らない |

### M4: 学習支援
| タスク | 内容 | 受け入れ条件 |
|--------|------|--------------|
| T-15 | 語彙帳（IndexedDB保存） | 会話で出た表現を保存 |
| T-16 | 復習機能 | 学んだ表現のテスト |
| T-17 | ピンイン・声調表示 | 返答に拼音・声調色分け |
| T-18 | 趣味ペルソナ充実 | 特殊な趣味にも対応 |

---

## 9. 品質ゲート

各フェーズの完了は、以下のすべてを満たすことを条件とする。
- 全タスクの受け入れ条件が満たされる
- `npm run build` が通る
- `npm test` が通る
- 自動レビュー合格
- 実機（モバイル）で主要フロー動作確認

---

## 10. 変更管理

- 仕様変更は **詳細仕様書・用語定義書・開発フロー** を先に更新し、その後にコードを変更する。
- 変更はコミットメッセージに `docs:` を付けて記録。
- 用語の追加・変更は用語定義書の §5 変更履歴に記録する。

---

## 11. トラブルシューティング

| 問題 | 対処 |
|------|------|
| LLM が構造化JSONを返さない | フォールバックで平文をJSON化、スキーマ再検証 |
| Codex のサンドボックスが動かない | `--sandbox danger-full-access` を使用（要慎重） |
| 音声 API がモバイルで動かない | Web Speech API のブラウザ差異を確認、フォールバック実装 |
| レート制限 | 指数バックオフでリトライ |
| キーがリークした | `wrangler secret put` で即時差し替え、履歴から削除 |

---

## 12. 本ドキュメントの位置づけ

- 本書は企画書・詳細仕様書・用語定義書とセットで運用する。
- 開発の進め方・ツール・タスク管理・デプロイの「やり方」を定義。
- 実行中に改善点を見つけたら、本書を更新してから運用を続ける。
